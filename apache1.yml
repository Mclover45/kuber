apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: raman1
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: app-default
    spec:
      containers:
      - name: app-default
        image: mclover45/firstimage
        env:
          - name: MSG
            value: Default Page
        ports:
        - name: http
          containerPort: 80

apiVersion: v1
kind: Service
metadata:
  name: ramanservice
  labels:
    app: app-default
spec:
  ports:
  - port: 80
  selector:
    app: app-default
